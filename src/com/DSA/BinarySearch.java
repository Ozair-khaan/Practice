package com.DSA;

import java.util.List;

public class BinarySearch {
    int search(List list, int target) {
        int first = 0;
        int last = list.size() - 1;

        while (first <= last) {
            int midPoint = (first + last) / 2; //5+20=2-->12.5

//            if(Collections.list(midPoint)==target){
//                return midPoint;
//            }
        }
        return 0;
    }
}
